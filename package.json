{
  "name": "my-project",
  "scripts": {
    "api": "npm run -w @my-project/api dev",
    "app": "npm run -w @my-project/app dev",
    "lint": "npm run -ws lint",
    "ts": "npm run -ws ts"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.15.0",
    "eslint": "^8.57.0",
    "nodemon": "^3.1.4"
  }
}
